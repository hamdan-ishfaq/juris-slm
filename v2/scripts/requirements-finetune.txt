# Local smoke test — standard path (transformers + bnb)
# Install PyTorch with CUDA first if needed (see README Phase 1.3).
transformers>=4.43.0,<5.10.0
datasets>=2.18.0
peft>=0.11.0,<0.20.0
bitsandbytes>=0.43.0
accelerate>=0.30.0,<2.0.0
trl>=0.15.0,<2.0.0
tqdm>=4.66.0
sentencepiece>=0.2.0
protobuf>=4.25.0,<8.0.0
